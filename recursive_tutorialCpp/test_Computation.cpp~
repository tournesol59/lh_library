#include "decl_Computation.hpp"

int main(int argc, char* argv[]) {
  std::vector<double> my_coeff;

   my_coeff.push_back(1.0);
   dNum = 5;
   dn = 1;

   ComputationBase inst_cpbase(my_coeff, dn, dNum);
   inst_cpbase.scandata();

  return 0;
}
